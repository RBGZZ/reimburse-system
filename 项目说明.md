# 项目说明

> 报销云 · 财务报销管理系统（可演示版）

---

## 一、项目概述

一套**可演示**的财务报销管理系统：员工在线提交发票与报销信息，财务在线审核，出纳一键打款，管理员全程管理与审计。全流程状态实时可见、操作留痕，并内置**管理员智能体**（管理后台对话 + DSH 智能体预设），直连 DeepSeek 真实大模型。

---

## 二、系统运行方式

### 2.1 环境要求
- Node.js **≥ 22.5**（本项目使用内置 `node:sqlite`；本机验证版本 v24.15）
- 无第三方依赖，**无需 `npm install`**

### 2.2 启动
```bash
cd "D:\manage project"
npm start        # 或 node server.js
```

### 2.3 访问
- 业务系统：**http://127.0.0.1:3300**
- 开发/智能体环境（DSH）：**http://127.0.0.1:3080**

### 2.4 说明
- 首次启动自动创建演示账号。
- 是否注入示例报销单由 `config.json` 的 `seedDemoData` 控制：`true` 注入、`false` 空库（当前为 `false`）。
- 智能体对话需在管理后台「智能问答 → 配置」填写 DeepSeek API Key，或设环境变量 `DEEPSEEK_API_KEY`。

---

## 三、测试账号

| 角色 | 用户名 | 姓名 | 密码 |
| --- | --- | --- | --- |
| 员工 1 | `zhangwei` | 张伟 | `123456` |
| 员工 2 | `lina` | 李娜 | `123456` |
| 财务审核 | `finance` | 王晓财务 | `123456` |
| 出纳 | `cashier` | 赵出纳 | `123456` |
| 管理员 | `admin` | 系统管理员 | `123456` |

> 登录页提供“一键填充”，点一下即填入账号（密码均 `123456`）。

---

## 四、开发本项目使用的模型与工具

### 4.1 模型
- **本项目开发用大模型**：来自 DeepSeek Harness（DSH）驱动模型
  - `provider: deepseek-official`
  - `model: deepseek-v4-flash-vision-exp`
  - `reasoningEffort: high`
- **项目内智能体默认模型**：`deepseek-v4-flash-vision-exp`（可在管理后台「智能问答 → 配置」修改，或环境变量 `DEEPSEEK_MODEL` 覆盖）

### 4.2 开发工具 / 环境
| 类别 | 工具 |
| --- | --- |
| 智能体开发环境 | DeepSeek Harness（DSH）Web GUI（`http://127.0.0.1:3080`） |
| 文件编辑 | `read` / `write` / `edit`、`glob`、`grep` |
| 命令执行 | `pwsh`（PowerShell），用于运行服务、语法校验、回归测试、无头浏览器截图验证 |
| 网页检索 | `web_search` / `web_fetch` |
| DSH 动态插件开发 | `cordis_inspect_*`、`cordis_define`、`cordis_run`、`cordis_undefine` 等（用于创建/校验 agent 预设） |
| 技能库 | `editing-cordis-compositions`（编辑 Cordis 组合）、`cordis-plugin-development`（开发动态插件） |
| UI 验证 | Microsoft Edge 无头浏览器（截图核对页面渲染） |

### 4.3 项目自身技术栈
- 后端：Node 内置模块（`http`、`node:sqlite`、`crypto`、`child_process`）
- 数据库：SQLite（`node:sqlite`）
- 前端：原生 HTML / CSS / JavaScript（单页应用）
- 智能体：DeepSeek 官方 API（OpenAI 兼容 / chat/completions）

---

## 五、Token 消耗量

本次项目的开发/调试合计 token 消耗量：**117,031,954**

---

## 六、其它

- 项目目录结构、功能、API、配置等详见同目录 `README.md`。
- 演示素材：`demo-invoices/` 两张发票；录制脚本见 `演示脚本.md`。
