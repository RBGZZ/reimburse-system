# 报销云 · 演示视频脚本

> 目的：让评审清楚看到「员工提交 → 数据隔离 → 财务审核(通过/驳回+原因) → 出纳打款 → 员工查看状态变化」的完整闭环。

## 一、准备

- 系统地址：**http://127.0.0.1:3300**
- 演示账号（密码均为 `123456`，登录页可“一键填充”）：
  - 员工1 `zhangwei`（张伟）
  - 员工2 `lina`（李娜）
  - 财务 `finance`（王晓财务）
  - 出纳 `cashier`（赵出纳）
  - 管理员 `admin`（系统管理员）
- 当前系统已设为**空报销记录**起点（5 个账号、0 条报销），评审能看到从零开始的完整过程。
- 两张演示发票图：`demo-invoices/`
  - `invoice-1-travel.png`（差旅高铁，¥586.00）
  - `invoice-2-office.png`（办公用品，¥238.50）

## 二、两张演示发票与填写数据

| 项目 | 发票A（张伟提交） | 发票B（李娜提交） |
| --- | --- | --- |
| 图片文件 | `invoice-1-travel.png` | `invoice-2-office.png` |
| 发票金额 | `586.00` | `238.50` |
| 发票内容 | `运输服务*客运服务费` | `办公用品*打印纸` |
| 申请事项 | `赴上海客户现场支持，往返高铁票` | `采购办公用品（A4 打印纸5包）` |

## 三、演示流程（按此顺序录制）

### 第 1 幕：员工张伟提交申请（发票A）
1. 登录页点「员工·张伟」一键填充 → 登录。
2. 首页为空（统计卡均为 0）。点右上 **＋新建报销**。
3. 表单：把 `invoice-1-travel.png` 拖进“发票图片”区（出现预览）→ 金额 `586.00` → 发票内容 `运输服务*客运服务费` → 申请事项 `赴上海客户现场支持，往返高铁票`。
4. 观察校验与反馈 → 提交 → Toast「报销申请已提交」。
5. 张伟列表出现该条 → 徽章 **待审核**。

> 解说词：员工填写发票信息、上传图片、提交；系统即时校验并生成“待审核”申请。

### 第 2 幕：员工李娜提交申请（发票B）
1. 右上角退出 → 登录页点「员工·李娜」→ 登录。
2. 同样**＋新建报销**：拖入 `invoice-2-office.png` → `238.50` → `办公用品*打印纸` → `采购办公用品（A4 打印纸5包）` → 提交。
3. 李娜列表出现该条 → 待审核。

### 第 3 幕：不同员工之间的数据隔离（重点）
1. 李娜退出 → 登录张伟：列表**只有发票 A 一条**，看不到李娜的发票 B。
2. 张伟退出 → 登录李娜：列表**只有发票 B 一条**，看不到张伟的发票 A。

> 解说词：员工只能看到自己提交的申请，他人的数据严格隔离。

### 第 4 幕：财务审核（看全部 + 通过 & 驳回原因）
1. 退出 → 登录财务 `finance`。
2. 明确看到**所有员工**的数据（张伟的 A 与李娜的 B 都出现，并显示“申请人”）。
3. 对发票A（张伟）：点 **通过** → Toast「审核通过，已进入打款池」，状态变 **已通过**。
4. 对发票B（李娜）：点 **驳回** → 弹窗**强制填写原因** → 填 `发票抬头不符，请开具公司抬头发票后重新提交` → 确认 → 状态变 **已驳回**，原因写入流程记录。

> 解说词：财务只能对待审核申请操作；驳回必须写明原因，便于员工整改后重新提交。

### 第 5 幕：出纳打款
1. 退出 → 登录出纳 `cashier`。
2. 「打款管理」默认显示 **已通过·待打款** 的发票A（¥586.00）。点 **打款** → 确认 → 状态 **已打款**。

> 解说词：出纳只能对审核通过的申请打款，不能修改内容或审核结论；重复打款会被系统拦截（幂等）。

### 第 6 幕：员工查看状态变化（实时）
1. 退出 → 登录张伟：发票A 已变 **已打款**；点开卡片看**流程时间线**：提交报销 → 审核通过 → 打款完成（含操作人、时间）。
2. 退出 → 登录李娜：发票B 已变 **已驳回**；点开看流程：提交报销 → 审核驳回（含驳回原因），并有 **修改并重提** 按钮。

> 解说词：状态变化实时可见（页面每 3 秒自动刷新并 Toast 提醒），全程留痕。

## 四、可选加分演示

- 登录**管理员 `admin`** → 管理后台：
  - **报销总览**（全量状态/金额统计）+ **导出 CSV**。
  - **操作审计**：能看到刚才每一步（谁的提交、谁审核、谁打款、驳回原因）。
  - **智能问答**：问“现在有多少已打款？”，答案来自真实模型。
- 演示“**驳回后修改并重提**”：用李娜账号打开已驳回的发票B → 「修改并重提」→ 改好后状态回到待审核。
- 演示“**重复提交提醒**”：再次提交相同金额+内容的发票，系统给出疑似重复提示。

## 五、录制建议

- 用一个浏览器窗口 + 录屏，逐幕“退出 → 登录另一个账号”，或提前开多个浏览器窗口分别登录更流畅。
- 每步说清三件事：**谁在操作、做了什么、系统给了什么反馈**。
- 强调关键词：**数据隔离 / 审批留痕 / 驳回原因 / 实时状态 / 权限职责分离**。
- 语速放缓，给评审看清状态徽章与流程记录的时间。
